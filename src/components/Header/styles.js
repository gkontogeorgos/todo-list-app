import { styled } from '@mui/material/styles';

export const HeaderBox = styled('div')({
  textAlign: 'center',
  top: 0,
  left: 0,
  width: '100%',
  zIndex: 99,
  background: '#cfcfcf',
  padding: 10,
});
